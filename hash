#include<stdio.h>
int a[100];
int main() {
	int n;
	scanf_s("%d", &n);
	int i, j, t;
	for (int i = 1; i <= n; i++) {
		scanf_s("%d", &t);
		a[t]++;
	}
	for (int i = 0; i < 100; i++) {
		for (int j = 0; j < a[i]; j++)
			printf("%d ", i);
	}
}
